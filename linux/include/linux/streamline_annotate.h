/* SPDX-License-Identifier: GPL-2.0 */
/* Stub — ARM Streamline annotations not available on RISC-V */
#ifndef _LINUX_STREAMLINE_ANNOTATE_H
#define _LINUX_STREAMLINE_ANNOTATE_H

#define ANNOTATE_CHANNEL_COLOR(ch, color, str) do {} while (0)
#define ANNOTATE_CHANNEL_END(ch) do {} while (0)
#define ANNOTATE_NAME_CHANNEL(group, ch, str) do {} while (0)
#define ANNOTATE_GREEN 0

#endif
